allow users to halt ai output by pressing the stop button

make the topbar consistent across all pages. Keep user login functionality for all topbar. adjust accordingly.
make top bar disappear when i scroll

only allow users to use app if they are signed in. this is to prevent spams. do not even allow them to submit a message
also, do not allow data to be saved into the database if user_id is blank (empty string) or null

fix animation on FAQ page

(optional: consider JWT)

PROJECT CREATION DATE: 4/20
